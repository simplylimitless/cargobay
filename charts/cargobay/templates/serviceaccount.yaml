# cargobay ServiceAccount
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cargobay.serviceAccountName" . }}
  labels:
    {{- include "cargobay.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
