# cargobay Service
apiVersion: v1
kind: Service
metadata:
  name: {{ include "cargobay.fullname" . }}
  labels:
    {{- include "cargobay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cargobay.selectorLabels" . | nindent 4 }}
